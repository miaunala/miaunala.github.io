// @ts-check
import { defineConfig } from 'astro/config';

// User/Org page (miaunala.github.io) -> serves at the root, so no `base` needed.
export default defineConfig({
  site: 'https://miaunala.github.io',
});
